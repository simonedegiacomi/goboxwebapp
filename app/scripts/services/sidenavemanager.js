'use strict';

/**
 * @ngdoc service
 * @name goboxWebapp.sidenaveManager
 * @description
 * # sidenaveManager
 * Service in the goboxWebapp.
 */
angular.module('goboxWebapp')
  .service('sidenaveManager', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
